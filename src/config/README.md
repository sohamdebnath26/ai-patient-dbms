# Configuration

Environment configuration and feature flags.

- `env.ts` — Validated environment variable access
- `featureFlags.ts` — Feature flag definitions and overrides
- `constants.ts` — Application-wide constants
